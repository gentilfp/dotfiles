# OS
.DS_Store

# Neovim — lazy-lock.json is machine-local (plugin versions resolve per machine)
nvim/lazy-lock.json
nvim/.nvimlog
nvim/doc/tags
*.swp
*.swo
*~

# Machine-specific ghostty overrides (window geometry etc.)
ghostty/local

# Claude
.claude/*
