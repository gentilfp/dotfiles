# Plugins
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-sensible'
set -g @plugin 'laktak/extrakto'
set -g @plugin 'nhdaly/tmux-better-mouse-mode'
set -g @plugin 'tmux-plugins/tmux-resurrect'
set -g @plugin 'pwittchen/tmux-plugin-spotify' # pip install spotify-cli-linux
set -g @plugin 'tmux-plugins/tmux-prefix-highlight'
set -g @plugin 'tmux-plugins/tmux-cpu'
set -g @plugin 'tmux-plugins/tmux-yank'
set -g @plugin 'tmux-plugins/tmux-copycat'
set -g @plugin 'alexwforsythe/tmux-which-key'
run '~/.tmux/plugins/tmux-which-key/plugin/init.tmux'
set -g @plugin 'christoomey/vim-tmux-navigator'
set -g @plugin 'tmux-plugins/tmux-continuum'
set -g @continuum-restore 'on'

# address vim mode switching delay (http://superuser.com/a/252717/65504)
set -s escape-time 0

# status bar
set-option -g status-right-length 200
set-option -g status-left-length 60
set -g status-bg "#282A36"
set -g status-fg "#F8F8F2"
set-option -g status-left "#[fg=#BD93F9,bold] #S "
set -g @prefix_highlight_fg '#282A36'
set -g @prefix_highlight_bg '#FF79C6'
set -g @prefix_highlight_show_copy_mode 'on'
set -g @prefix_highlight_copy_mode_attr 'fg=#282A36,bg=#F1FA8C'
set-option -g status-right "#{prefix_highlight} #[fg=#50FA7B]CPU:#{cpu_percentage} RAM:#{ram_percentage} #[fg=#6272A4]| #[fg=#F8F8F2]%d %b %H:%M #[nobold]"

# window status
setw -g window-status-current-style fg=#F8F8F2,bg=#44475A,bold,noitalics
set -g window-status-current-format " #I: #W "
set -g window-status-format "#[fg=#6272A4] #I: #W "

# zsh as default shell
set-option -g default-shell /bin/zsh

# Color
set -g default-terminal "screen-256color"
set-option -ga terminal-overrides ",xterm-256color:Tc"

# Use Alt-arrow keys to switch panes
bind -n M-Left select-pane -L
bind -n M-Right select-pane -R
bind -n M-Up select-pane -U
bind -n M-Down select-pane -D

# Balance panes
bind -n C-M-h select-layout even-vertical
bind -n C-M-v select-layout even-horizontal

# Shift arrow to switch windows
bind -n S-Left previous-window
bind -n S-Right next-window

# Swap windows with arrow
bind-key -n C-S-Left swap-window -t -1\; select-window -t -1
bind-key -n C-S-Right swap-window -t +1\; select-window -t +1

# Resize panes
bind-key -n M-S-Down resize-pane -D
bind-key -n M-S-Up resize-pane -U
bind-key -n M-S-Left resize-pane -L
bind-key -n M-S-Right resize-pane -R

# Mouse mode
set-option -g mouse on

# Make scrolling with wheels work
bind -n WheelUpPane if-shell -F -t = "#{mouse_any_flag}" "send-keys -M" "if -Ft= '#{pane_in_mode}' 'send-keys -M' 'select-pane -t=; copy-mode -e; send-keys -M'"
bind -n WheelDownPane select-pane -t= \; send-keys -M

# Set easier window split keys
bind-key v split-window -h
bind-key h split-window -v

# Easy config reload
bind-key r source-file ~/.tmux.conf \; display-message "~/.tmux.conf reloaded."

# Start windows and panes at 1, not 0
set -g base-index 1
setw -g pane-base-index 1

# Reload windows indexes
bind-key R movew -r

# Send to all panes on/off
bind-key ! setw synchronize-panes on
bind-key @ setw synchronize-panes off

# Initialize TMUX plugin manager (keep this line at the very bottom of tmux.conf)
run '~/.tmux/plugins/tpm/tpm'
